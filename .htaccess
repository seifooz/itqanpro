# Force correct MIME type for M4A audio files
AddType audio/mp4 .m4a
AddType audio/x-m4a .m4a

# Enable CORS for audio files (in case of cross-origin issues)
<FilesMatch "\.(m4a|mp3|ogg|wav)$">
    Header set Access-Control-Allow-Origin "*"
</FilesMatch>

# Prevent directory listing
Options -Indexes
